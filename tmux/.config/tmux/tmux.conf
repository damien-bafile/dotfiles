# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'jaclu/tmux-menus'
set -g @plugin '27medkamal/tmux-session-wizard'
set -g @plugin 'sainnhe/tmux-fzf'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'tmux-plugins/tmux-battery'
set -g @plugin 'hendrikmi/tmux-cpu-mem-monitor'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-copycat'
set -g @plugin 'tmux-plugins/tmux-open'

set -g extended-keys on
set -g default-terminal "screen-256color"
set -as terminal-features ",xterm-256color:RGB"

# Battery settings with Catppuccin colors
set -g @batt_icon_status_charged ""
set -g @batt_icon_status_charging ""
set -g @batt_icon_status_discharging ""
set -g @batt_icon_status_attached ""
set -g @batt_icon_status_unknown "?"

# Battery charge tier icons (Powerline, universally supported)
set -g @batt_icon_charge_tier1 '🔋'
set -g @batt_icon_charge_tier2 '🔋'
set -g @batt_icon_charge_tier3 '🔋'
set -g @batt_icon_charge_tier4 '🔋'
set -g @batt_icon_charge_tier5 '🔋'
set -g @batt_icon_charge_tier6 '🔋'
set -g @batt_icon_charge_tier7 '🔋'
set -g @batt_icon_charge_tier8 '🔋'

# Battery charging tier icons
set -g @batt_icon_charging_tier1 '⚡'
set -g @batt_icon_charging_tier2 '⚡'
set -g @batt_icon_charging_tier3 ''
set -g @batt_icon_charging_tier4 ''
set -g @batt_icon_charging_tier5 '⚡'
set -g @batt_icon_charging_tier6 '⚡'
set -g @batt_icon_charging_tier7 '⚡'
set -g @batt_icon_charging_tier8 '⚡'

# Battery color thresholds (Catppuccin colors)
set -g @batt_color_high "#a6e3a1"      # Green (>50%)
set -g @batt_color_medium "#f9e2af"    # Yellow (20-50%)
set -g @batt_color_low "#f38ba8"       # Red (<20%)

# Force battery background to match status bar base color
set -g @batt_color_charge_secondary_tier1 "#1e1e2e"
set -g @batt_color_charge_secondary_tier2 "#1e1e2e"
set -g @batt_color_charge_secondary_tier3 "#1e1e2e"
set -g @batt_color_charge_secondary_tier4 "#1e1e2e"
set -g @batt_color_charge_secondary_tier5 "#1e1e2e"
set -g @batt_color_charge_secondary_tier6 "#1e1e2e"
set -g @batt_color_charge_secondary_tier7 "#1e1e2e"
set -g @batt_color_charge_secondary_tier8 "#1e1e2e"

set-environment -g TMUX_PLUGIN_MANAGER_PATH '~/.config/tmux/plugins/'
# Prefix
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# Splits
unbind '"'
unbind %
bind \\ split-window -h
bind - split-window -v

# Reload config
bind r source-file ~/.config/tmux/tmux.conf

# Vim like pane selection
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Resize panes
bind -n M-h resize-pane -L 1
bind -n M-l resize-pane -R 1
bind -n M-j resize-pane -D 1
bind -n M-k resize-pane -U 1
bind -n M-H resize-pane -L 5
bind -n M-L resize-pane -R 5
bind -n M-J resize-pane -D 5
bind -n M-K resize-pane -U 5

# Mouse
set -g mouse on

# Menus
set -g @menus_trigger 'a'
set -g @menus_location_x 'C'
set -g @menus_location_y 'C'

# Start window and pane numbering at 1
set -g base-index 1
setw -g pane-base-index 1
set-window-option -g pane-base-index 1
set-option -g renumber-windows on

# Window navigation
bind -n S-Left previous-window
bind -n S-Right next-window
bind -n M-1 select-window -t 1
bind -n M-2 select-window -t 2
bind -n M-3 select-window -t 3
bind -n M-4 select-window -t 4
bind -n M-5 select-window -t 5
bind -n M-6 select-window -t 6
bind -n M-7 select-window -t 7
bind -n M-8 select-window -t 8
bind -n M-9 select-window -t 9

# Vi copy mode
set-window-option -g mode-keys vi
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi C-c send-keys -X rectangle-toggle
bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel
unbind -T copy-mode-vi MouseDragEnd1Pane

# Status bar
set -g status-position top

# Catppuccin colours
base="#1e1e2e"
mauve="#cba6f7"
lavender="#b4befe"
sapphire="#74c7ec"
text="#cdd6f4"
green="#a6e3a1"

set -g status-left-length 50
set -g status-right-length 50
set -g status-style "fg=#cba6f7,bg=${base},fg=${text}"
set -g status-left "#[fg=${green},bg=${base},bold] #h #[fg=${sapphire}]:: #[fg=${green}]#(~/.config/tmux/scripts/ip_status.sh) "
set -g status-justify absolute-centre
set -g status-right "#[bg=${base}]#(~/.config/tmux/scripts/battery_wrapper.sh)#[bg=${base},fg=${text}] #[fg=${text}]#(~/.config/tmux/scripts/cpu_status.sh) #[fg=${sapphire}]:: #[fg=${text}]#(~/.config/tmux/scripts/mem_status.sh)"
set -g window-status-current-format "#[fg=${mauve},bg=${base},bold]#I:#W"
set -g window-status-format "#[bg=${base}] #I:#W"
set -g message-style "fg=${lavender},bg=${base}"
set -g mode-style "fg=${base},bg=${mauve}"
set -g pane-border-style "fg=${text}"
set -g pane-active-border-style "fg=${mauve}"

# Menus styling
set -g @menus_simple_style_selected "fg=${base},bg=${mauve}"
set -g @menus_simple_style "bg=${base}"
set -g @menus_simple_style_border "bg=${base}"
set -g @menus_border_type 'rounded'
set -g @menus_nav_next "#[fg=${green}]-->"
set -g @menus_nav_prev "#[fg=${green}]<--"
set -g @menus_nav_home "#[fg=${green}]<=="

# Resurrect & Continuum
set -g @resurrect-strategy-nvim 'session'
set -g @resurrect-capture-pane-contents 'on'
set -g @continuum-restore 'off'
set -g @continuum-save-interval '10'

set -g extended-keys-format csi-u

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.config/tmux/plugins/tpm/tpm'
